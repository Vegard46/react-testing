import React from 'react';
import '../styles/project-page.css';

type TimelineArtefactProps = {
    date: string
}

function TimelineArtefact (props: TimelineArtefactProps) {
    return (
        <div className="timeline-artefact">
            TimeLineArtefact
        </div>
    )
}

export default TimelineArtefact;